{% extends 'base.html.twig' %}

{% block body %}
    <div class="p-1">
        {{ form_start(feedback) }}
        {{ form_row(feedback.EmailHash) }}
        {{ sample|raw }}
        {{ form_row(feedback.send) }}
        {{ form_end(feedback) }}
    </div>
    <script type="text/javascript">
        window.parent.postMessage({
            'func': 'parentFunc',
            'message': 'Message text from iframe.'
        }, "*");
    </script>
{% endblock %}
